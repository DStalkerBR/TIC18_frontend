import { AnimatorDirective } from './animator.directive';

describe('AnimatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatorDirective();
    expect(directive).toBeTruthy();
  });
});
